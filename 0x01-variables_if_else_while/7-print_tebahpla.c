#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: "print all aplhabet letters in lowercase in reverse order"
 *
 * Return: Always 0 (Success)
*/

int main(void)
{
	char ch = 'z';

	while (char >= 'a')
	{
		putchar(ch);
		ch--;
	}
	putchar('\n');
	return (0);
}
