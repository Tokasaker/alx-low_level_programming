#include "main.h"

/**
 * _abs - function to check if caharcter is lowercase
 *
 * @n: absloute value of n
 *
 * Return: Always 0 (Success)
*/

int _abs(int n)
{
	if (n < 0)
		n = (-1) * n;
	return (n);
}
